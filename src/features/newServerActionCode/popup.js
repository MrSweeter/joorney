import PopupFeature from '../../generic/popup.js';
import configuration from './configuration.js';

export default class NewServerActionCodePopupFeature extends PopupFeature {
    constructor() {
        super(configuration);
    }
}
