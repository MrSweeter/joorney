import { PopupCustomizableFeature } from '../../generic/popup.js';
import configuration from './configuration.js';

export default class FEATURE_CLASS_NAMEPopupFeature extends PopupCustomizableFeature {
    constructor() {
        super(configuration);
    }

    async render(enabled) {
        TODO('Complete empty generated method for popup customization rendering');
    }
}
