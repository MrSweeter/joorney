import { BackgroundTriggerContentFeature } from '../../generic/background.js';
import configuration from './configuration.js';

export default class FEATURE_CLASS_NAMEContentFeature extends BackgroundTriggerContentFeature {
    constructor() {
        super(configuration);
    }
}
