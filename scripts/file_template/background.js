import BackgroundFeature from '../../generic/background.js';
import configuration from './configuration.js';

export default class FEATURE_CLASS_NAMEBackgroundFeature extends BackgroundFeature {
    constructor() {
        super(configuration);
    }

    async loadFeature(tab, url, args) {
        TODO('Complete empty generated method for background');
    }
}
